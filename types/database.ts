/**
 * PLACEHOLDER — run `npm run db:types` after linking your Supabase project
 * (see README) to generate the real, fully-typed version of this file from
 * supabase/migrations/0001_init.sql. Everything currently typed `any` below
 * becomes a precise type once that command runs.
 */
export type Database = any;
